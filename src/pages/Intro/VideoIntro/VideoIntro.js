import classNames from "classnames/bind";
import style from './VideoIntro.module.scss';

const cx = classNames.bind(style);

function VideoIntro() {
    return (  
        <div className={cx('VideoIntro')}>

        </div>
    );
}

export default VideoIntro;